/* TODO
const mongoose = require('mongoose')
const Schema = mongoose.Schema

const UsuarioSchema = new Schema({
    nome: String,
    dataNascimento: 
})

module.exports = mongoose.model('Postagem', UsuarioSchema)
*/
